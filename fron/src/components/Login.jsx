import React from "react";
import { useState } from "react";
const Login = ()=>{
    const [data, setData] = useState(null);// Objeto que contiene el email y la contraseña
    return(
        <div>
            <h1>Login</h1>
        </div>
    );
}
export default Login;